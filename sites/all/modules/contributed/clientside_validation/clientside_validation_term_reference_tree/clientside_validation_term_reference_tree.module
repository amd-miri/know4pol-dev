<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function clientside_validation_term_reference_tree_ctools_plugin_directory($module, $plugin) {
  if ($module == 'clientside_validation') {
    return 'plugins/' . $plugin;
  }
}
