imports:
  - { resource: vendor/openeuropa/code-review/dist/drupal-conventions.yml }
parameters:
  tasks.phpcs.ignore_patterns:
    - build/
    - vendor/
  tasks.phpmd.exclude:
    - build/
    - vendor/
